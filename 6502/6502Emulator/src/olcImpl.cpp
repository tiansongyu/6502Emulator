// olcPixelGameEngine / olcPGEX_Sound 的唯一实现编译单元。
// 两个头的实现块位于 include guard 之外，由下面两个宏开启；
// 全工程必须有且仅有这一个 TU 定义它们。
#define OLC_PGE_APPLICATION
#include "olcPixelGameEngine.h"

#define OLC_PGEX_SOUND
#include "olcPGEX_Sound.h"
